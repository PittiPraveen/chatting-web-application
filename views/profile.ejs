<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.15/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-900 text-white">
    <div class="container mx-auto mt-10 p-4">
        <form action="/profile" method="POST">
            <div class="mb-4">
              <label for="profile-picture" class="block text-white">Change Profile Picture</label>
              <input
                type="text"
                id="profile-picture"
                name="profilePicture"
                class="w-full p-2 border bg-gray-700 rounded"
                placeholder="Enter Image URL"
              />
            </div>
            <div class="mb-4">
              <label for="password" class="block text-white">Change Password</label>
              <input
                type="text"
                id="password"
                name="password"
                class="w-full p-2 border bg-gray-700 rounded"
                placeholder="Enter New Password"
                value="<%= password %>"
              />
            </div>
            <button class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded">Save</button>
          </form>
          
      </div>
    </div>
  </body>
</html>
